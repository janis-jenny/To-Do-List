


export default class Project {
  constructor(name) {
    this.name = name;
    this.tasks = [];
  }

  /* addProject() {
    storage.push({
      id: uniqid(),
      name: this.name,
      tasks: this.tasks,
    });
    console.log(storage);
  }

  renderProject() {
    this.addProject();
  } */
}
