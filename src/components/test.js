

/* function showForm() {
  const showTodoForm = document.querySelector('.task-btn');
  showTodoForm.addEventListener('click', (e) => {
    e.preventDefault();
    const todoForm = document.querySelector('#todos-form');
    todoForm.toggle();
  });
} */
/* 
addProjectToForm(project) {
  if (!storage) return false
  const projectListselect = document.getElementById('project-list');
  #iterar el storage para agregarle a los options
  storage.map(item =>item.name)
  const option = document.createElement('option');
  option.value = project.name;
  option.selected = 'selected';
  option.id = project.name;
  option.innerHTML = project.name;
  projectListselect.append(option);
  console.log(project);
  return project
}
 */